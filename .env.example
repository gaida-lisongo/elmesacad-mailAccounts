DB_HOST=localhost
DB_NAME=servermail
DB_USER=mailuser
DB_PASS=admin
MAIL_SERVER_FQDN=mail.inbtp.ac.cd
IMAP_PORT=993
SMTP_PORT=465
PORT=3000
# Clé API (en-tête X-API-Key) — obligatoire en production pour /mail-accounts et /mail-aliases
ACCOUNT_API_KEY=
